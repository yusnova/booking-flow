# Manual test cases — booking flow

## How this pack maps to typical assessment criteria

| Criterion | What “good” looks like here | How the catalogue supports it |
| --- | --- | --- |
| **Functional correctness** | Postcode rules, API contracts, waste branching, skip disable logic, review maths, confirm behaviour match the written spec and fixtures. | Positive paths (MT-01, MT-04, MT-12–MT-14, MT-23–MT-27, MT-37–MT-40), API checks (MT-08–MT-11, MT-33–MT-38), and state rows verify transitions and persistence. |
| **Product realism** | Copy, empty states, errors with retry, disabled skips with reasons, price breakdown, and “manual address when list is empty” feel like a credible UK hire journey—not a toy with only happy paths. | EC1A / BS1 / M1 fixtures (MT-08–MT-09, MT-18–MT-19), heavy waste disable rules (MT-16, MT-24), plasterboard variants (MT-25–MT-26), review pricing (MT-27), manual flow (MT-06–MT-07, MT-28–MT-29). |
| **Manual testing depth** | Enough breadth: positives, negatives, edges, API failures, and state changes—with enough detail that another tester can repeat without guessing. | Category counts in the summary table meet or exceed common brief minimums; preconditions and steps below are explicit so **depth** is in the instructions, not only in the count. |

---

## Catalogue

| ID | Title | Category | Preconditions | Steps | Expected result |
| --- | --- | --- | --- | --- | --- |
| MT-01 | Valid SW1A lookup returns list | Positive | **Environment:** `npm run dev` (or deployed build) reachable. **Browser:** Desktop Chrome or Firefox, current stable. **State:** Open `/`, hard refresh so no prior booking session. **Data:** None entered yet—you are on step 1 with empty postcode field. | 1) Open the app root URL. 2) Click into **UK postcode**. 3) Type `SW1A 1AA` (any reasonable casing). 4) Click **Look up**. 5) Wait until loading text disappears or list/error appears. 6) Scroll the address list if it is taller than the viewport. 7) Count distinct selectable address rows. | At least **12** address rows; the **first** row is selected by default; first line reads like **10 Downing Street**; **Continue** is enabled. **Do not** see the client validation error **Enter a valid UK postcode (e.g. SW1A 1AA).** — that message is only for invalid-format postcodes, not for this successful lookup. |
| MT-02 | Invalid postcode rejected | Negative | Same server as MT-01. **State:** Step 1. **Product:** User enters a string that fails the UK postcode pattern (lookup is not called with a valid token). | 1) Enter `NOTAPOST` (or another string that fails UK pattern). 2) Click **Look up**. 3) Observe error text and whether any address list renders. 4) Observe **Continue** state. | The UI shows **Enter a valid UK postcode (e.g. SW1A 1AA).** (client-side validation before the API). **No** address list from a successful lookup; **Continue** stays disabled until a valid postcode + address path is completed. |
| MT-03 | Postcode with extra spaces | Edge | Step 1; network normal. **Intent:** Prove normalisation, not user perfection. | 1) Enter leading/trailing and double spaces, e.g. ` sw1a  1aa `. 2) Click **Look up**. 3) After success, read displayed postcode in UI if shown. | **Unlike MT-02:** the value is still a valid UK postcode after trimming/space collapse — **do not** see **Enter a valid UK postcode (e.g. SW1A 1AA).** Lookup **succeeds** (same as MT-01: address list, **Continue** can be enabled after selection). Where the app shows the postcode (e.g. review), it appears **formatted** as **`SW1A 1AA`**, not the raw string with extra spaces. |
| MT-04 | Select another address | Positive | **After** successful SW1A lookup on step 1; list visible. **Goal:** Change default selection before continuing. | 1) Click radio for **`addr_5`** (or another non-default row). 2) Confirm only one radio is selected. 3) Click **Continue**. 4) Complete waste + skip minimally to reach review (general + any enabled skip is enough). 5) On review, read the **Address** line. | Step 2 loads; later **review** summary shows the **chosen** line1 text for `addr_5`, not the default first row. |
| MT-05 | Continue before lookup | Negative | Step 1; user has **not** called the API yet for the current value. | 1) Type a valid postcode e.g. `SW1A 1AA`. 2) **Do not** click **Look up**. 3) Try **Continue**. | **Continue** is **disabled** (or click has no effect); user cannot skip the lookup step. |
| MT-06 | Manual address empty | Negative | **Fixture:** `EC1A 1BB` (zero addresses). **State:** After lookup, empty state visible; **Enter address manually** checked. | 1) Lookup `EC1A 1BB`. 2) Enable manual address if not auto-enabled. 3) Clear the manual textarea completely. 4) Attempt **Continue**. | **Continue** disabled until manual text exceeds minimum length rule. |
| MT-07 | Manual address too short | Edge | Same as MT-06 after EC1A lookup; manual mode on. | 1) Type only `abc` in manual box. 2) Try **Continue**. 3) Add more characters until above demo threshold. 4) Try **Continue** again. | Short text blocks continue; once length rule satisfied, **Continue** enables. |
| MT-08 | BS1 first lookup 500 | API failure | **Fixture knowledge:** `BS1 4DJ` first request returns 500 in demo. **Tools:** Network tab open optional. | 1) Enter `BS1 4DJ`. 2) Click **Look up** **once**. 3) Read UI message and HTTP status in Network. 4) Do **not** retry yet. | HTTP **500** (or surfaced as service error); user sees **error** state with **Retry** (or equivalent); address list not shown as success. |
| MT-09 | BS1 retry succeeds | API failure | **Immediately after** MT-08 on same session, without restarting server. | 1) Click **Retry lookup** (or **Look up** again per UI). 2) Wait for completion. | Second attempt returns **200** with a **non-empty** address list; user can proceed. |
| MT-10 | Skips API without postcode | API failure | Any REST client or browser address bar; **no** UI required. | 1) `GET /api/skips?postcode=` (empty value) or omit postcode. 2) Read status and JSON body. | **400** with JSON error indicating missing/invalid postcode. |
| MT-11 | Confirm missing fields | API failure | REST client; `Content-Type: application/json`. | 1) `POST /api/booking/confirm` with body `{}`. 2) Read response. | **400**; body mentions missing required field(s) (e.g. `skipSize` / `postcode` per implementation). |
| MT-12 | Step 1 → 2 | State transition | SW1A looked up successfully; default or any address selected; still on step 1. | 1) Click **Continue**. 2) Observe step header / dots. 3) Confirm waste cards visible. | Step **2** visible; progress indicator highlights step 2; postcode step UI hidden or inactive. |
| MT-13 | Step 2 → 1 back | State transition | On step 2 with waste screen visible; came from MT-12. | 1) Click **Back**. 2) Observe step indicator. 3) Check postcode field and address list still present. | Returns to step **1**; prior postcode and address selection **retained** (unless product spec says otherwise). |
| MT-14 | Plasterboard without handling option | Negative | Step 2; **Plasterboard** path selected; **no** radio chosen under handling. | 1) Click **Plasterboard** tile. 2) Leave the three handling radios unset. 3) Click **Continue**. | Error message; remain on step 2; no navigation to skips. |
| MT-15 | Waste API blocked | Negative | Step 2; DevTools **Network** → **Request blocking** or offline for `/api/waste-types` only. | 1) Pick any waste path. 2) Click **Continue** while POST is blocked or fails. 3) Read UI feedback. | User-visible error (not silent failure); still on step 2 or clear recoverable state. |
| MT-16 | Disabled skip not selectable | Negative | Step 3; **Heavy** waste selected from step 2; skip list loaded. | 1) Locate **12-yard** row. 2) Try to click its radio. 3) Try keyboard activation if applicable. | Radio **disabled**; selection does not move to 12-yard; helper/disabled text visible. |
| MT-17 | No double confirm | Negative | Step 4; valid review; **Confirm booking** enabled. | 1) Double-click **Confirm booking** quickly (or mash click). 2) Count success screens or duplicate IDs if shown. | Only **one** booking completes; button shows busy/disabled during submit; single **BK-** reference. |
| MT-18 | M1 slow lookup | Edge | **Fixture:** `M1 1AE` introduces artificial delay. **Tool:** Optional stopwatch. | 1) Enter `M1 1AE`. 2) Click **Look up**. 3) Time from click until list or final error appears. | Loading indicator visible; total wait **≥ ~2s** before result; eventually list or error resolves. |
| MT-19 | EC1A empty copy | Edge | None beyond app running. | 1) Lookup `EC1A 1BB`. 2) Read empty-state messaging. 3) Read guidance for manual address. | **Zero** addresses; empathetic copy; manual path obvious; user understands next action. |
| MT-20 | Step 3 → 2 keeps waste | State transition | Step 3 reached with **Heavy** selected on step 2. | 1) Click **Back** from skips. 2) On step 2, observe which waste tile is highlighted. | **Heavy** still selected; no silent reset to General. |
| MT-21 | Step 4 → 3 keeps skip | State transition | Step 4 review visible; **6-yard** (or chosen skip) selected on step 3. | 1) Click **Back** from review. 2) On step 3, check selected skip. | Previously selected skip size **still** selected. |
| MT-22 | No skip, no review | Negative | Step 3; if something pre-selected, clear by choosing a disabled row impossible—instead arrive with no selection if UI allows, else skip setup: use fresh session and navigate to step 3 without clicking a skip (if UI permits). **Alt:** verify **Review** disabled when `skipSize` unset. | 1) Reach step 3 without selecting any enabled skip (if possible). 2) Look at **Review booking** button. | **Review booking** **disabled** until a valid skip is chosen. |
| MT-23 | General skips all usable | Positive | Step 3; **General** waste; SW1A postcode used earlier. | 1) Open each skip row. 2) Check disabled flags vs heavy-only rules. | All eight sizes **enabled** for general (no false disabled for this profile). |
| MT-24 | Heavy disables two sizes | Positive | Step 3; **Heavy** waste. | 1) Find **12-yard** and **14-yard** rows. 2) Read disabled reason text. 3) Pick an enabled size and continue. | Both rows **disabled** with visible reason; at least one smaller skip remains **enabled**. |
| MT-25 | Plasterboard mixed | Positive | Step 2: **Plasterboard** + **Less than 10% mixed** handling. | 1) Continue to step 3. 2) Inspect disabled pattern (should not wrongly disable entire list). | Skips load; not all sizes disabled; behaviour matches demo rule for mixed plasterboard. |
| MT-26 | Plasterboard dedicated | Positive | Step 2: **Plasterboard** + **Dedicated** handling. | 1) Continue to step 3. 2) Check **2-yard** and **3-yard** rows. | **2-yard** and **3-yard** **disabled**; larger sizes available for selection. |
| MT-27 | Price breakdown | Positive | Step 4 with any valid skip price. | 1) Expand/read **Price breakdown** block. 2) Manually add skip + permit + VAT in a calculator. 3) Compare to **Total** line. | Subcomponents show **£**; arithmetic matches **Total** within rounding shown in UI. |
| MT-28 | Script in manual address | Negative | Manual address path active; review reachable. | 1) Paste `<script>alert(1)</script>` into manual address. 2) Continue to review. 3) Observe DOM / behaviour. | Text shown **literally** on review; **no** script execution / alert. |
| MT-29 | Very long manual text | Negative | Manual path; textarea accepts paste. | 1) Paste ~4000+ characters. 2) Scroll UI. 3) Attempt continue and confirm if allowed. | Layout remains usable (scroll); app does not freeze; validation or confirm behaves predictably. |
| MT-30 | Keyboard to retry | Edge | After a failed lookup with retry link (e.g. BS1 first fail). | 1) Press **Tab** repeatedly from postcode field until focus reaches **Retry**. 2) Activate with **Enter**. | Retry reachable **without mouse**; second attempt runs. |
| MT-31 | Start again clears | State transition | **After** successful confirmation screen with booking id. | 1) Click **Start new booking**. 2) Inspect step 1 fields and selections. | Back to step **1**; postcode/address/waste/skip cleared or reset to initial demo state. |
| MT-32 | Step dots track progress | State transition | Full happy path from step 1 through success. | 1) After each **Continue**, note which step dot is highlighted. 2) On **Back**, note dot regression. | Highlighted dot always matches **current** step number on screen. |
| MT-33 | Malformed confirm body | API failure | REST client. | 1) `POST /api/booking/confirm` with body `{{{` or truncated JSON. 2) Read status. | **400** invalid JSON (or clear server error), not **500** hang. |
| MT-34 | Normalisation helper | Edge | Step 3; general path; **4-yard** enabled. | 1) Scroll to normalisation demo control. 2) Click control that applies `"4 yard"` → `4-yard` selection. 3) Confirm radio state. | **4-yard** becomes selected when enabled; no error. |
| MT-35 | Manual toggle | Edge | SW1A list visible after lookup. | 1) Toggle **Enter address manually** on: textarea appears, list selection clears per spec. 2) Toggle off: list selection restored if spec says so. 3) Repeat once. | No orphaned selection; **Continue** enable/disable matches rules after each toggle. |
| MT-36 | Empty postcode lookup | Negative | Step 1; field cleared. | 1) Clear postcode completely. 2) Click **Look up**. | Validation message; no pretend success. |
| MT-37 | Waste-types contract | Positive | REST client; valid JSON body per API doc. | 1) `POST /api/waste-types` with `{ "heavyWaste": false, "plasterboard": false, "plasterboardOption": null }`. 2) Read body. | **`{ "ok": true }`** and **200**. |
| MT-38 | Skips query contract | API failure | Browser or curl; URL encoded query. | 1) `GET /api/skips?postcode=SW1A1AA&heavyWaste=true`. 2) Parse JSON. | **200**; `skips` array length **8**; each item has `size`, `price`, `disabled`. |
| MT-39 | Switch waste before continue | State transition | Step 2; user changes mind before leaving. | 1) Click **General**. 2) Click **Heavy** without continuing yet. 3) Click **Continue**. 4) On step 3, verify large skips disabled. | Step 3 rules match **last** selected waste (**Heavy**), not the first click. |
| MT-40 | Change plasterboard option | State transition | Step 2; plasterboard path; user changes handling before continue. | 1) Select **Dedicated** then switch to **Mixed** (still on step 2). 2) Click **Continue**. 3) On step 3, compare disabled rows to dedicated-only case from memory or second run. | Skip list reflects **Mixed** rules after continue; user does not see dedicated-only disables for mixed (re-run MT-26 if needed to compare). |

---

## Summary counts (vs typical brief minimums)

| Category | Count in this pack |
| --- | ---: |
| Total | 40 |
| Negative | 11 |
| Edge | 7 |
| API failure | 6 |
| State transition | 8 |
| Positive | 8 |
